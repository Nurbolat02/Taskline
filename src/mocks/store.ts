
// In-memory данные для верстки без Postgres. Живут в module-scope, поэтому
// переживают несколько запросов в рамках одного dev-процесса, но сбрасываются
// при перезапуске `next dev` — это ожидаемо, для верстки достаточно.

// ---- Categories ----

// ---- Tasks ----

// ---- Sessions ----

// ---- Activity log ----

